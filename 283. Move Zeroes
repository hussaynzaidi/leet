void moveZeroes(int* nums, int numsSize) {
    int left = 0;

    for (int i = 0; i < numsSize; i++) {
        if (nums[i] != 0) {
            nums[left++] = nums[i];
        }
    }
    while (left < numsSize) {
        nums[left++] = 0;
    }
}
